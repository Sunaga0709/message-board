<h1>メッセージ一覧</h1>

<ul>
  <% @messages.each do |message| %>
    <li>
      <%= link_to message.id, message_path(message) %> :
      <%= message.context %>
    </li>
  <% end %>
</ul>

<%= link_to '新規作成', new_message_path %>